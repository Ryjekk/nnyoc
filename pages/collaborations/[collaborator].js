import GlobalWrapper from "../../components/layout/ GlobalWrapper";
import Layout from "../../components/layout/Layout";

const Collaborator = () => {
    return (
        <GlobalWrapper>
            <Layout contentType='CollaboratorContent'/>
        </GlobalWrapper>
    );
};

export default Collaborator;